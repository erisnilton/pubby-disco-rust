[package]
name = "domain"
version = "0.1.0"
edition = "2021"

[dependencies]
entity = { path = "../../macros/entity" }
shared = { path = "../shared" }
chrono = { workspace = true }
serde = { workspace = true }
